<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dannextech.apps.hotelsystem">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginForm"></activity>
        <activity android:name=".CreateNewUser" />
        <activity android:name=".SignInForm" />
        <activity android:name=".PaymentList" />
        <activity android:name=".OrderedMealList"></activity>
        <activity android:name=".ViewOrderedMeals" />
        <activity
            android:name=".FinalSystem.WaiterNavigationDrawer"
            android:label="Waiter"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FinalSystem.CookNavigationDrawer"
            android:label="@string/title_activity_cook_navigation_drawer"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".MakeOrder"></activity>
    </application>

</manifest>